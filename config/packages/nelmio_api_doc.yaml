nelmio_api_doc:
    documentation:
        info:
            title: links.johnnoel.uk
            description: Stores and organises links and bookmarks
            version: 0.0.1
        components:
            securitySchemes:
                HttpBasic:
                    type: http
                    scheme: basic
    areas: # to filter documented areas
        default:
            path_patterns:
                - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
